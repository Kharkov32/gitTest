extends layout

include mixins/_adminStoreProduct

block content
  .inner
    h2= title
    p(style="color: red;") WARNING!!! Everything you do on this page is permanent AND NON-REVERSIBLE

    if store.products.length > 0
      .products
        each product in store.products
          +admiStoreProduct(product)
    else
      p No products